import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Enable static exports for easy deployment
  // output: 'export', // Uncomment this for static site generation
}

export default nextConfig
